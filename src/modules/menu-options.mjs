import { Search12Regular as SearchIcon } from "@vicons/fluent";
import * as Tools from "@/modules/tools.mjs";

const mainMenuOptions = [
  {
    label: "Search",
    key: "search",
    icon: Tools.renderIcon(SearchIcon),
  }
];

export { mainMenuOptions };